# MINI-CSS-PROJECT
This project is a simple sidebar navigation menu built using HTML and CSS. The sidebar opens smoothly when the user hovers over the menu icon and closes when the cursor is moved away. It uses basic CSS concepts like positioning, transitions, and hover effects.
